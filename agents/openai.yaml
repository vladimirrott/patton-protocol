interface:
  display_name: "Patton Protocol"
  short_description: "Coordinate bounded coding-agent missions"
  default_prompt: "Use $patton-protocol to scope, plan, dispatch, observe, reconcile, verify, and report bounded coding-agent missions."

policy:
  allow_implicit_invocation: true
