# Fixture

This README links to a [file that does not exist](does-not-exist.md), to
prove the validator resolves README's own links, not just SKILL.md's.

It also links to an existing [directory](adapters/), which must NOT be
flagged as broken.
