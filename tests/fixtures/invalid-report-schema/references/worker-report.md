# Worker Report Contract (fixture)

This fixture intentionally omits two required report fields to test the
validator's missing-report-field diagnostic.

## Required fields

mission identity, status, files changed, commands run, evidence, risks,
content digest, candidate tracked paths, candidate untracked paths,
non-candidate untracked paths.
